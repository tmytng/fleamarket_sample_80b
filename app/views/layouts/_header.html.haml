.pcHeader
  .headerInner
    .mainHeader
      .icon
        = link_to "#" do
          = image_tag 'logo/logo.png', alt: 'ロゴ'
      .searchBox
        -# = form_with model: @product do |f|
        -#   = f.hidden_field :カラム名, value: "✓"
        -#   = f.text_field :name, class:'#', :placeholder => "メールアドレス", :type => "email", :value => ""
        -#   = f.submit "検索する"
            .searchBox__icon
              = image_tag 'icon/icon-search1.png', alt: '検索'
      .resultLists
    .nav
      .listsLeft
        .listsLeft__item.listsLeft__item--first
          = link_to "カテゴリー", "#", class: "categoryBtn"
          .categoryField.displayNone
            .parentsList
              - @parents.each do |parent|
                .parentsList--item
                  = link_to "#{parent.name}", item_path(parent), class: "parentCategory", id: "#{parent.id}"
            .childrenList
              .childrenList--item
            .grandChildrenList
              .grandChildrenList--item
        .listsLeft__item
          = link_to "ブランド", "#", class: "brandBtn"
      .listsRight
        - if user_signed_in?
          .listsRight__item.listsRight__item--logout
          = link_to "ログアウト", destroy_user_session_path, method: :delete
        - else
          .listsRight__item.listsRight__item--login
            = link_to "ログイン", "#"
          .listsRight__item.listsRight__item--new
            = link_to "新規会員登録", "#"
